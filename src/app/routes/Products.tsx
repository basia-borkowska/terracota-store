import { PageWrapper } from '../../shared/ui/layout/PageWrapper';

import ProductList from '../../widgets/ProductList';

const Products = () => {
  return (
    <PageWrapper>
      {/* TODO add categories section and breadcrumbs */}
      <ProductList />
    </PageWrapper>
  );
};

export default Products;
